# 摩迪 · mody-app

C 端一体化应用（乘客 / 司机），同一手机号可双身份，个人中心可切换。

- **项目名**：mody-app（原 user-app 已改名）
- **应用名**：摩迪
- **运行**：`npm install` → `npm start`（终端1）→ `npm run android`（终端2）；USB 真机时可用 `adb reverse tcp:8081 tcp:8081` 后重载

 UI 风格与根目录 `modi-*.html` 文档一致（深色主题、卡片、图标元素）。
